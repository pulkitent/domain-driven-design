package com.thoughtworks.ddd.account_service.application_service_account.listener;

public class UpdateAddressListener {
    //read the msg from bus
    // call the account app service (if failed then raise a compensating event to customerservice)
}
